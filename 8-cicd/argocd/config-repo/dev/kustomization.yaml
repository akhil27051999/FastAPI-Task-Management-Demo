apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: task-management-dev

resources:
  - ../../../../7-kubernetes-python

patchesStrategicMerge:
  - replica-patch.yaml
  - resource-patch.yaml

images:
  - name: task-management-api-python
    newTag: dev-latest
  - name: task-management-frontend
    newTag: dev-latest
