apiVersion: v1
kind: Service
metadata:
  name: task-frontend-service
  namespace: task-management-python
  labels:
    app: task-frontend
spec:
  selector:
    app: task-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
